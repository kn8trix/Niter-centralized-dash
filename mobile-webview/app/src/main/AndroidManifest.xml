<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Networking — the app is a WebView over HTTPS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Native permissions: camera + media (profile photos, prescription
         uploads, reports attachments), notifications (FCM push), and the
         emergency siren (VIBRATE + WAKE_LOCK for a high-priority alert). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- CAMERA implies the android.hardware.camera feature — declare it as
         NOT required so devices without a camera can still install the app. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.NiterDash">

        <!-- Splash — the launcher entry point. Shows the "NITER Campus Hub —
             Student Edition" splash, then hands off to the WebView shell. -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.NiterDash.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Native QR scanner — bypasses WebView getUserMedia for attendance scanning. -->
        <activity
            android:name=".ScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density"
            android:launchMode="singleTask" />

        <!-- Firebase Cloud Messaging — receives emergency + campus pushes.
             Safe to ship without google-services.json: the service simply
             never receives messages until Firebase is configured. -->
        <service
            android:name=".EmergencyMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Stop Siren action on the emergency notification — a plain
             receiver (no UI), registered so the notification action can
             stop the looping siren without opening the app. -->
        <receiver
            android:name=".SirenControlReceiver"
            android:exported="false" />
    </application>

</manifest>
