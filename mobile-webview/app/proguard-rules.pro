# Release build rules. The WebView wrapper uses no reflection or serialization,
# so the default Android rules are sufficient — add project-specific rules here
# if the app grows (e.g. JSON libraries).
