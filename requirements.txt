django>=4.2,<5.0
django-allauth>=0.61,<0.70
# allauth only declares PyJWT + cryptography + requests(-oauthlib) under its
# `socialaccount` extra, so a plain `pip install django-allauth` skips them and
# the Google provider's jwtkit import fails at startup
# (`ModuleNotFoundError: No module named 'jwt'`). The `[crypto]` extra installs
# `cryptography` for the RS256 key handling.
PyJWT[crypto]>=2.8,<3.0
requests>=2.31
requests-oauthlib>=1.3.1,<3.0
google-api-python-client>=2.100
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2
gspread>=6.0

# Real-time notification & system alert engine (WebSockets)
channels>=4.0,<5.0
# ASGI server used by the Render start command (config.asgi:application — HTTP
# + WebSockets). Pinned below 5.0 to stay compatible with channels 4.x.
daphne>=4.0,<5.0
# Optional WSGI server for self-hosted deployments (e.g. a gunicorn systemd
# unit); the Render blueprint starts daphne directly and does not use it.
gunicorn>=21.2,<23.0

# Implicit dependencies of channels/daphne, pinned explicitly so fresh installs
# (CI / Render / local) never depend on transitive resolution order.
# channels requires asgiref>=3.9.0; daphne requires asgiref>=3.5.2,
# autobahn>=22.4.2 and twisted[tls]>=22.4 (the TLS extra pulls pyOpenSSL +
# service-identity + cryptography).
asgiref>=3.9,<4.0
autobahn>=22.4.2,<27
twisted[tls]>=22.4,<27

# Background task queue (async TF-IDF note analysis). Redis-backed; the
# [django-redis] extra pulls in redis-py so the worker and the channel layer
# share the same REDIS_URL.
huey[django-redis]>=2.5,<3.0

# Allow-list HTML sanitizer for the Website Builder (core/block_sanitizer.py)
# — strips scripts/event handlers/unsafe URL schemes from authored block HTML.
bleach>=6.0,<7.0

# Research AI — server-side document text extraction (services/document_text.py)
# for reference PDFs/DOCX uploaded to the OpenRouter-backed research assistant.
# pypdf parses .pdf files; python-docx reads .docx paragraph text.
pypdf>=4.0,<6.0
python-docx>=1.1,<2.0

# Production deployment & configuration
# Environment-driven settings (secrets, DB URL, hosts) via a local .env
django-environ>=0.11
# Static asset serving in production (no external CDN needed)
whitenoise>=6.6
# Redis-backed channel layer for multi-worker real-time notifications
channels-redis>=4.2
# PostgreSQL driver - required by the managed database (DATABASE_URL) on Render
psycopg2-binary>=2.9.9
# Parses SUPABASE_DB_URL / DATABASE_URL into Django's DATABASES config
# (mapped via OPTIONS so sslmode=require works with Supabase's TLS-only pools).
dj-database-url>=2.0
